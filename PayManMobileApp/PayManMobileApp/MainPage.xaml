<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:PayManMobileApp"
             x:Class="PayManMobileApp.MainPage">

    <ContentPage.BindingContext>
        <local:PayManViewModel />
    </ContentPage.BindingContext>


      <StackLayout>
            <Frame BackgroundColor="Green" Padding="40" CornerRadius="0">
                <Label Text="Profil" HorizontalTextAlignment="Center" TextColor="White" FontSize="36"/>
            </Frame>


                <ListView x:Name="PayMan" ItemsSource="{Binding PayManItems}" >

                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Label Text="{Binding PayManText}" FontSize="Large" />

                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>

                </ListView>

            <Frame >
                <Entry
                    x:Name="InputField"
                    Text="{Binding NewPayManValue}"
                    Placeholder="Indtast nyt job"
                    ReturnCommand="{Binding AddPayManCommand}"
                />
            </Frame>

      </StackLayout>

   
           

</ContentPage>
